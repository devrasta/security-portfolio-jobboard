meta {
  name: 2FA Enable
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/2fa/enable
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "code": "123456"
  }
}
